Progress, two landed increments (commits 5f4a7ce54 + 088e89dca):

INCREMENT 1 - the serial binding BOOTS (ACs 1, 2, 5):
- FleetHost::boot stops re-raising the tier refusal: the serial projection hosts the same slot FSM; the FF-T3 binding seam instantiates the serial seat model.
- SeatHost::boot_serial: the same pooled queue/host loop/FSM slots, but ONE named cycle thread (work-fleet-serial-0) runs every granted unit in grant order; census rows print logical. Intake stays the 10-never-drop shape - no second waiting policy (6HE6RF): saturation is the advisory queue depth through the census serial rows.
- FleetSolveExecutor::boot_serial: the keyed-mailbox construction with the projection ONE solver seat named serial-0 (boot_seats splits naming from the shared construction).
- AC 1 pinned by test (32 units to receipts on the named seat; the callable itself reports work-fleet-serial-0). AC 5 pinned by test (forced pinned on 4 cores runs marked-oversubscribed). AC 2 rides the 6HE6RF amendment shape (no bounded-intake second policy).
- Stale-pending artifacts retired: the FF-T3 seam tests flipped, the sub-floor refusals moved below the serial floor (BelowHostFloor), the FF-T1 python seam child shrinks to ONE CPU, the station xfail retired (the seat names are binding-aware).

INCREMENT 2 - the DECIDED option (a) prerequisite (AC 3's input):
- PostureCause::LaneDeath + EnterReason::LaneDeath + observe_cause on the machine AND owner: immediate enter (no hysteresis), OWN exit discipline (the clean window never lifts it - sticky until a fresh process, no in-process clear API by design), throttle-cordon upgrades turn sticky, PostureCounters.lane_deaths, the same section-6 cordon effects. 6 new tests (31 posture total).

REMAINING (NOT done):
- AC 3 detection + terminal receipts: the host-side lane-death tripwire (seat death - the host notices, cordons via observe_cause, synthesizes typed Failed records for the in-flight solve paths - the QR3NUS lane-witness synthesis) and the live-process host-loop survival path. The typed input + sticky discipline are LANDED; the wiring is not.
- AC 4: the cross-binding corpus parity parametrization (pinned vs serial over the same capture corpus - FF-T5 folds the profile-parametrized replay).

Gates at both commits: cargo test -p degenbot-bot -p degenbot-workers (713+123, then 713+129), just test-python baseline (2680/21), clippy, fmt.
